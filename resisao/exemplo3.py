from math import *
b = float(input
         "Digte o valor do cateto B: ")
c = float(input
          ("Digte o valor do cateto C: "))
a = (sqrt((pow(b.2)+pow(c,2))))
print(f"A hipoetnusa é igual a: {a:.2f}")
